@PACKAGE_INIT@

include(ChronoCoreConfig)
include(ChronoRobotConfig)
include(ChronoModelVehicleConfig)
if(@ENABLE_MODULE_CASCADE@)
  include(ChronoCascadeConfig)
endif()
if(@ENABLE_MODULE_COSIMULATION@)
  include(ChronoCosimulationConfig)
endif()
if(@ENABLE_MODULE_IRRLICHT@)
  include(ChronoIrrlichtConfig)
endif()
if(@ENABLE_MODULE_MATLAB@)
  include(ChronoMatlabTargets)
endif()
if(@ENABLE_MODULE_PARDISO_MKL@)
  include(ChronoPardisoMKLConfig)
endif()
if(@ENABLE_MODULE_MUMPS@)
  include(ChronoMumpsConfig)
endif()
if(@ENABLE_MODULE_MULTICORE@)
  include(ChronoMulticoreTargets)
endif()
if(@ENABLE_MODULE_OPENGL@)
  include(ChronoOpenGLConfig)
endif()
if(@ENABLE_MODULE_POSTPROCESS@)
  include(ChronoPostprocessConfig)
endif()
if(@ENABLE_MODULE_PYTHON@)
  include(ChronoPyparserConfig)
endif()
if(@ENABLE_MODULE_VEHICLE@)
  include(ChronoVehicleConfig)
endif()
if(@ENABLE_MODULE_FSI@)
  include(ChronoFSIConfig)
endif()
if(@ENABLE_MODULE_GPU@)
  include(ChronoGPUConfig)
endif()
if(@ENABLE_MODULE_SENSOR@)
  include(ChronoSensorConfig)
endif()
if(@ENABLE_MODULE_PARDISO_PROJECT@)
  include(ChronoPardisoProjectConfig)
endif()
if(@ENABLE_MODULE_SYNCHRONO@)
  include(ChronoSynchronoConfig)
endif()
if(@ENABLE_MODULE_MODAL@)
  include(ChronoModalTargets)
endif()
if(@ENABLE_MODULE_DISTRIBUTED@)
  include(ChronoDistributedConfig)
endif()
if(@ENABLE_MODULE_CSHARP@)
  include(ChronoCSharpConfig)
endif()